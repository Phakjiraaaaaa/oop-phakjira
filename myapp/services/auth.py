from myapp.utils.helpers import hash_password

def login(username: str, password_hash: str) -> bool:
    """ฟังก์ชันเข้าสู่ระบบ"""
    print("🔐" + "─" * 35)
    if username == "alice" and password_hash == hash_password("secret"):
        print(f"🎉 เข้าสู่ระบบสำเร็จ 🎉")
        print(f"ยินดีต้อนรับกลับมา คุณ {username}!")
        print("─" * 37)
        return True
    else:
        print("❌ เข้าสู่ระบบไม่สำเร็จ ❌")
        print("กรุณาตรวจสอบชื่อผู้ใช้หรือรหัสผ่านอีกครั้ง")
        print("─" * 37)
        return False


def logout(user) -> None:
    """ฟังก์ชันออกจากระบบ"""
    print("👋" + "─" * 35)
    print(f"ล็อกเอาท์ผู้ใช้ {user.username} เรียบร้อยแล้ว")
    print("─" * 37)
