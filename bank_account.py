class LibraryBook:
    def __init__(self):
        self._status = "available"  # สถานะเริ่มต้น

    @property
    def status(self):
        return self._status

    def borrow(self):
        if self._status == "available":
            self._status = "borrowed"
            print("ยืมหนังสือสำเร็จ! สถานะตอนนี้:", self._status)
        else:
            print("ไม่สามารถยืมหนังสือได้ หนังสือถูกยืมไปแล้ว")

    def return_book(self):
        if self._status == "borrowed":
            self._status = "available"
            print("คืนหนังสือสำเร็จ! สถานะตอนนี้:", self._status)
        else:
            print("หนังสือยังไม่ถูกยืม ไม่ต้องคืน")


# 🔹 ตัวอย่างการทดสอบ
book = LibraryBook()
print("สถานะเริ่มต้น:", book.status)

book.borrow()       # ยืมหนังสือ
book.borrow()       # พยายามยืมซ้ำ
book.return_book()  # คืนหนังสือ
book.return_book()  # พยายามคืนซ้ำ
